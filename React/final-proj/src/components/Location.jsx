import SuspectPage from "./SuspectPage";
const Location = ({tab, data}) => {
    return ( 
        <div>
            <SuspectPage tab={tab} data={data}/>
        </div>
     );
}
 
export default Location;